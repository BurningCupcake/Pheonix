// Importing Foundation framework, it provides fundamental services needed by Swift applications and frameworks
import Foundation

// Defining an enumeration EyeState to model the different states an eye can be in
enum EyeState {
    case open     // Represents the state when the eye is open
    case closed   // Represents the state when the eye is closed
}
