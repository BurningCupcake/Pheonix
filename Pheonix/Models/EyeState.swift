//This is a simple enumeration called EyeState that represents the state of an eye. It has two cases: .open and .closed.

//open: Represents an open eye state.
//closed: Represents a closed eye state.
//This enum can be used to track and differentiate between the states of eyes in various eye-tracking or gaze detection scenarios.

import Foundation

enum EyeState {
    case open
    case closed
}
